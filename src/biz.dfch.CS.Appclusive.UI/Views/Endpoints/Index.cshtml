@using biz.dfch.CS.Appclusive.UI.Helpers
@model IEnumerable<biz.dfch.CS.Appclusive.UI.Models.Diagnostics.Endpoint>
@using biz.dfch.CS.Appclusive.UI.App_LocalResources
@{
    ViewBag.Title = "Endpoint" + " list";
}
@section Title{
    <h2>@GeneralResources.ResourceManager.GetString("Endpoint")@GeneralResources.List</h2>
}
<div class="row">
    @Html.Partial("SearchForm")
</div>
<table class="table table-striped table-hover">
    <tr>
        <th>
            @Html.DisplayNameForSort(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameForSort(model => model.Version)
        </th>
        <th>
            @Html.DisplayNameForSort(model => model.RoutePrefix)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td onclick="window.location = '@Url.Action("Details", new { id = item.Id })'" class="ap-hand">
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td onclick="window.location = '@Url.Action("Details", new { id = item.Id })'" class="ap-hand">
                @Html.DisplayFor(modelItem => item.Version)
            </td>
            <td onclick="window.location = '@Url.Action("Details", new { id = item.Id })'" class="ap-hand">
                @Html.DisplayFor(modelItem => item.RoutePrefix)
            </td>
            <td class="ap-btn text-nowrap">
                @Html.SecuredButton("Read", Url.Action("Details", new { id = item.Id }))
            </td>
        </tr>
    }

</table>
